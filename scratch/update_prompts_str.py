import sqlite3

def update_all_prompts_to_string():
    db_path = 'backend/experiments.db'
    conn = sqlite3.connect(db_path)
    cursor = conn.cursor()

    prompts = {
        "0": """这是超级吴英混调器控制屏幕。
你需要判断当前是自动还是手动模式，并提取屏幕上对应的数值。

【自动模式识别】：左侧菜单栏标有 "自动" 的选项（通常带有一个 (A) 图标）处于高亮/被选中状态时。
此时屏幕中央上方会显示“段一”、“段二”、“段三”三个表格。
请提取以下数值（必须作为字符串输出）：
- 模式 (mode): "自动" (固定填这个中文字符串)
- 段一转速 (seg1_speed): "段一"表格中"转速(转)"下方的数值
- 段一时间 (seg1_time): "段一"表格中"时间(S)"下方的数值
- 段二转速 (seg2_speed): "段二"表格中"转速(转)"下方的数值
- 段二时间 (seg2_time): "段二"表格中"时间(S)"下方的数值
- 段三转速 (seg3_speed): "段三"表格中"转速(转)"下方的数值
- 段三时间 (seg3_time): "段三"表格中"时间(S)"下方的数值
- 总共时长 (total_time): "总共时长(S)"右侧的数值
- 剩余时长 (remaining_time): "剩余时长(S)"右侧的数值
- 当前段数 (current_segment): "当前段数"右侧的数值
- 当前转速 (current_speed): "当前转速(转)"右侧的数值

【手动模式识别】：左侧菜单栏标有 "手动" 的选项（带有一个手指标志）处于高亮/被选中状态时。
屏幕中央会显示“高速”和“低速”的表格，请提取：
- 模式 (mode): "手动" (固定填这个中文字符串)
- 高速转速 (high_speed): "高速"行的转速
- 高速时间 (high_time): "高速"行的设置时间
- 低速转速 (low_speed): "低速"行的转速
- 低速时间 (low_time): "低速"行的时间
- 当前转速 (current_speed): 下方实时转速

【重要】严格按以下格式输出 JSON，使用中文键名，所有的数值必须用双引号包裹作为字符串输出。无法读取的设为 null（不要加引号）：
自动模式示例：{"模式": "自动", "段一转速": "10000", "段一时间": "20", "段二转速": "16000", "段二时间": "20", "段三转速": "22000", "段三时间": "20", "总共时长": "60", "剩余时长": "1", "当前段数": "1", "当前转速": "0"}
手动模式示例：{"模式": "手动", "高速转速": "0", "高速时间": "0", "低速转速": "0", "低速时间": "0", "当前转速": "0"}

注意：
- 只输出 JSON，所有提取的数值都要作为文本字符串。绝对不要包含任何单位（如 转、S 等）。
- 绝不要输出 Markdown 代码块，不要解释你的推理过程。""",

        "1": """这是电子分析天平1号（SN: 53662），读取屏幕显示的重量数值。

【重要】严格按以下JSON格式输出，使用中文键名，不要输出任何其他内容。读取到的数字必须作为字符串（带双引号）输出：
{"重量": "填入你看到的数字文本"}

注意：
1. 仔细辨认小数点位置（LED数码管上小数点非常细小，通常在最后两位数字之前）。
2. 如果看到 4033，请根据常识判断它应该是 40.33，并且输出为字符串 "40.33"。如果屏幕显示 84.2，则输出 "84.2"。
3. 只输出纯数字的文本字符串，不含单位，不要照抄模板里的文字。""",

        "2": """这是电子天平2号（SN: 230199），读取屏幕显示的重量数值。

【重要】严格按以下JSON格式输出，使用中文键名，不要输出任何其他内容。读取到的数字必须作为字符串（带双引号）输出：
{"重量": "填入你看到的数字文本"}

注意：
1. 仔细辨认小数点位置（LED数码管上小数点很小）。
2. 只输出纯数字的文本字符串，不含单位。绝对不要照抄模板里的文字，必须输出屏幕上真实的数字作为文本。""",

        "3": """这是 PH 计（酸度计）的液晶屏幕。
请读取屏幕上最大的三个数字，并将它们作为字符串输出：
1. PH值 (ph_value): 屏幕中间最大的数字，通常带 2 位或 3 位小数（如 7.01 或 4.005）。
2. 温度 (temperature): 屏幕下方带 °C 或 MTC 字样的数字（如 25.0）。
3. PTS (pts): 屏幕下方百分号旁的数字（通常为 100.0 或 98.5）。

【重要】严格按以下 JSON 格式输出，使用中文键名，所有的数值必须用双引号包裹作为字符串输出：
{"PH值": "0.00", "温度": "0.0", "PTS": "0.0"}

注意：只输出纯数字构成的字符串 JSON，不要含单位，无法读取设为 null（不要加引号）。""",

        "4": """这是水质检测仪（SN: 43373），检测总硬度。请先判断当前是高量程还是低量程模式，然后读取屏幕显示的所有数值，并将数值作为字符串输出。

【重要】严格按以下JSON格式输出，使用中文键名，所有的数值必须用双引号包裹作为字符串输出：
{"模式": "高量程", "检测日期": "", "空白值": "0", "检测值": "0", "吸光度": "0.000", "含量": "0.00", "透光度": "0.0"}

注意：模式字段填入"高量程"或"低量程"，检测日期字段为字符串（格式xxxx-xx-xx xx:xx:xx），其他字段均为纯数字文本，无法读取设为null。只输出一行JSON。""",

        "5": """这是表界面张力仪控制屏幕。
请读取屏幕上显示的以下六个参数数值，并将它们作为字符串输出：
1. 张力 (tension)：屏幕上方最大的数字，单位 mN/m。可能带有负号，通常有3位小数。
2. 温度 (temperature)：带 °C 单位的数值。若显示 N/A 则为 null。
3. 上层密度 (upper_density)：单位 g/cm3。
4. 下层密度 (lower_density)：单位 g/cm3。
5. 上升速度 (rise_speed)：单位 mm/min。
6. 下降速度 (fall_speed)：单位 mm/min。

【重要】严格按以下 JSON 格式输出，使用中文键名，所有的数值必须用双引号包裹作为字符串输出：
{"张力": "0.000", "温度": "0.0", "上层密度": "0.000", "下层密度": "0.000", "上升速度": "0", "下降速度": "0"}

注意：
- 只输出纯数字的文本字符串，绝对不要包含任何字母或单位。
- 如果某个值由于未测量而显示为空白或 N/A，请将其值设为 null（不要加引号）。
- 屏幕上的 F: - / + 是按钮操作，不是数值的正负号，请勿混淆。""",

        "6": """这是电动搅拌器（SN: 208721），屏幕显示三行数值：第一行转速、第二行张力、第三行时间。
请将所有提取的数值作为字符串输出。

【重要】严格按以下JSON格式输出，使用中文键名，不要输出任何其他内容：
{"转速": "0", "张力": "0", "时间": "00:00"}

注意：时间字段保留MM:SS字符串格式；张力可能显示为00表示0N/cm。只输出纯数字文本字符串，不含单位。""",

        "7": """这是水浴锅（SN: 37844），读取屏幕显示的温度和定时时间，作为字符串输出。

【重要】严格按以下JSON格式输出，使用中文键名，所有的数值必须用双引号包裹作为字符串输出：
{"温度": "0.0", "时间": "0"}

注意：TEMP标签下方为温度（通常带1位小数，LED数码管小数点很小，如"17.3"），TIME标签下方为时间（整数分钟）。只输出纯数字文本字符串，不含单位。""",

        "8": """这是6速旋转粘度计（SN: 106833），读取屏幕上的八个数值，作为字符串输出。

【重要】严格按以下JSON格式输出，使用中文键名，所有的数值必须用双引号包裹作为字符串输出：
{"实施读数": "0", "最大读数": "0", "最小读数": "0", "转速": "0", "剪切速率": "0", "剪切应力": "0.000", "表观粘度": "0.0", "5秒平均值": "0.0"}

只输出一行JSON，纯数字文本，不含单位，无法读取设为null。"""
    }

    for inst_id, prompt in prompts.items():
        cursor.execute("UPDATE instrument_templates SET prompt_template = ? WHERE instrument_type = ?", (prompt, inst_id))
    
    conn.commit()
    conn.close()
    print("All Prompts updated to explicitly request STRING outputs.")

if __name__ == "__main__":
    update_all_prompts_to_string()
