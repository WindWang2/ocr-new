import sqlite3

def update_all_prompts_zh():
    db_path = 'backend/experiments.db'
    conn = sqlite3.connect(db_path)
    cursor = conn.cursor()

    prompts = {
        "0": """这是超级吴英混调器控制屏幕。
你需要判断是自动还是手动模式，并提取数值。

【自动模式识别】：左侧菜单栏 "自动模式" 选项高亮时。
提取以下数值，如果屏幕上没有，请填入 null：
- 模式 (mode): "自动" (固定填这个中文字符串)
- 段一转速 (seg1_speed)
- 段一时间 (seg1_time)
- 段二转速 (seg2_speed)
- 段二时间 (seg2_time)
- 段三转速 (seg3_speed)
- 段三时间 (seg3_time)
- 总时长 (total_time)
- 剩余时长 (remaining_time)
- 当前段数 (current_segment)
- 当前转速 (current_speed)

【手动模式识别】：左侧菜单栏 "手动模式" 选项高亮时。
屏幕中央有表格，提取：
- 模式 (mode): "手动" (固定填这个中文字符串)
- 高速转速 (high_speed): "高速"行的转速
- 高速时间 (high_time): "高速"行的设置时间
- 低速转速 (low_speed): "低速"行的转速
- 低速时间 (low_time): "低速"行的时间
- 当前转速 (current_speed): 下方实时转速

【重要】严格按以下格式输出 JSON，使用中文键名：
自动：{"模式": "自动", "当前转速": 0, "剩余时长": 0, "总时长": 0, "段一转速": 0, "段二转速": 0, "段三转速": 0}
手动：{"模式": "手动", "高速转速": 0, "高速时间": 0, "低速转速": 0, "低速时间": 0, "当前转速": 0}

只输出 JSON，不要输出 Markdown 块或解释。""",

        "1": """这是电子分析天平1号（SN: 53662），读取屏幕显示的重量数值。

【重要】严格按以下JSON格式输出，使用中文键名，不要输出任何其他内容：
{"重量": 0.00}

注意：
1. 仔细辨认小数点位置（LED数码管上小数点非常细小，通常在最后两位数字之前）。
2. 请务必确认小数点！如果看到 4033，请根据常识判断它应该是 40.33。
3. 数值单位为g，只输出纯数字不含单位。""",

        "2": """这是电子天平2号（SN: 230199），读取屏幕显示的重量数值。

【重要】严格按以下JSON格式输出，使用中文键名，不要输出任何其他内容：
{"重量": 0.00}

注意：仔细辨认小数点位置（LED数码管上小数点很小），数值单位为g，只输出纯数字不含单位。""",

        "3": """这是 PH 计（酸度计）的液晶屏幕。
请读取屏幕上最大的三个数字：
1. PH值 (ph_value): 屏幕中间最大的数字，通常带 2 位或 3 位小数（如 7.01 或 4.005）。
2. 温度 (temperature): 屏幕下方带 °C 或 MTC 字样的数字（如 25.0）。
3. PTS (pts): 屏幕下方百分号旁的数字（通常为 100.0 或 98.5）。

【重要】严格按以下 JSON 格式输出，使用中文键名，不要输出任何其他内容：
{"PH值": 0.00, "温度": 0.0, "PTS": 0.0}

注意：只输出纯数字 JSON，不要含单位，无法读取设为 null。""",

        "4": """这是水质检测仪（SN: 43373），检测总硬度。请先判断当前是高量程还是低量程模式，然后读取屏幕显示的所有数值。

【重要】严格按以下JSON格式输出，使用中文键名，不要输出任何其他内容：
{"模式": "高量程", "检测日期": "", "空白值": 0, "检测值": 0, "吸光度": 0.000, "含量": 0.00, "透光度": 0.0}

注意：模式字段填入"高量程"或"低量程"，检测日期字段为字符串（格式xxxx-xx-xx xx:xx:xx），其他字段为数值，无法读取设为null。只输出一行JSON。""",

        "6": """这是电动搅拌器（SN: 208721），屏幕显示三行数值：第一行转速、第二行张力、第三行时间。

【重要】严格按以下JSON格式输出，使用中文键名，不要输出任何其他内容：
{"转速": 0, "张力": 0, "时间": "00:00"}

注意：时间字段保留MM:SS字符串格式；张力可能显示为00表示0N/cm。只输出一行JSON，数值不含单位。""",

        "7": """这是水浴锅（SN: 37844），读取屏幕显示的温度和定时时间。

【重要】严格按以下JSON格式输出，使用中文键名，不要输出任何其他内容：
{"温度": 0.0, "时间": 0}

注意：TEMP标签下方为温度（通常带1位小数，LED数码管小数点很小，如"17.3"），TIME标签下方为时间（整数分钟）。只输出一行JSON，数值不含单位。""",

        "8": """这是6速旋转粘度计（SN: 106833），读取屏幕上的八个数值。

【重要】严格按以下JSON格式输出，使用中文键名，不要输出任何其他内容：
{"实施读数": 0, "最大读数": 0, "最小读数": 0, "转速": 0, "剪切速率": 0, "剪切应力": 0.000, "表观粘度": 0.0, "5秒平均值": 0.0}

只输出一行JSON，数值不含单位，无法读取设为null。"""
    }

    for inst_id, prompt in prompts.items():
        cursor.execute("UPDATE instrument_templates SET prompt_template = ? WHERE instrument_type = ?", (prompt, inst_id))
    
    conn.commit()
    conn.close()
    print("All Prompts updated successfully with Chinese keys.")

if __name__ == "__main__":
    update_all_prompts_zh()
